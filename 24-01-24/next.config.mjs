/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['images.unsplash.com', 'img.icons8.com', 'd1lss44hh2trtw.cloudfront.net']
  }
};

export default nextConfig;
